spring.application.name=Plantita-BackEnd

# --- Configuración de Base de Datos (Conexión segura con Render) ---
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Configuración JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update

spring.datasource.hikari.data-source-properties.sslmode=require

# Mostrar las consultas SQL en la consola (útil para depurar GET y POST)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Configuración del Servidor 
server.address=0.0.0.0
server.port=${PORT:8080}

# --- Configuración de Swagger/OpenAPI 
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html

# Credenciales fijas de seguridad para pruebas
spring.security.user.name=user
spring.security.user.password=$2a$12$gnbFZKeMju9S5G4sH/hLiuz3dNA5j1kNDlC/lzOY.bs0mQmu2ugpi

# Evita errores si alguna clase está vacía al serializar a JSON
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
